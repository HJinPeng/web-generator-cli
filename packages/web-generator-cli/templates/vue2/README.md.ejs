# <%= projectName %>

## Project setup
```bash
<%= npmUtil === 'npm' ? 'npm run' : npmUtil %> install
```

### Compiles and hot-reloads for development
```bash
<%= npmUtil === 'npm' ? 'npm run' : npmUtil %> dev
# or
<%= npmUtil === 'npm' ? 'npm run' : npmUtil %> serve
```

### Compiles and minifies for production
```bash
<%= npmUtil === 'npm' ? 'npm run' : npmUtil %> build
```

### Lints and fixes files
```bash
<%= npmUtil === 'npm' ? 'npm run' : npmUtil %> lint
```

### Customize configuration
See [Configuration Reference](https://cli.vuejs.org/config/).
